function main() {
    // The following is intentially badly written code. The purpose of this
    // is to see if the correctness metric calculations detects this as bad code. 
    const baba = "baba"
    const str1 = old_str1.replaceAll(baba, "    ");
}